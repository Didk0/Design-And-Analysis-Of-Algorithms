#include <iostream>

int main()
{
    // условие: https://leetcode.com/problems/maximum-subarray/description/

    /*int currSum = 0, maxSum = nums[0];

    for (int i = 0; i < nums.size(); i++)
    {
        currSum += nums[i];
        if (currSum > maxSum)
        {
            maxSum = currSum;
        }

        if (currSum < 0)
        {
            currSum = 0;
        }
    }

    return maxSum;*/

    return 0;
}
