#include <iostream>

int main()
{
    /*int lens[2501];

    for (int i = 0; i < nums.size(); i++)
    {
        lens[i] = 1;
        for (int j = i - 1; j >= 0; j--)
        {
            if (nums[i] > nums[j])
            {
                lens[i] = max(lens[i], lens[j] + 1);
            }
        }
    }

    int res = 1;
    for (int i = 0; i < nums.size(); i++)
    {
        res = max(res, lens[i]);
    }

    return res;*/

    return 0;
}
