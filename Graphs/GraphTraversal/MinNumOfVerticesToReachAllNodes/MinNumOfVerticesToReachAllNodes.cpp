#include <iostream>

int main()
{
    // условие: https://leetcode.com/problems/minimum-number-of-vertices-to-reach-all-nodes/description/

    /*int arr[100005] = { 0, };
    for (int i = 0; i < edges.size(); i++)
    {
        arr[edges[i][1]]++;
    }
    vector<int> sources;
    for (int i = 0; i < n; i++)
    {
        if (arr[i] == 0)
            sources.push_back(i);
    }
    return sources;*/
}
